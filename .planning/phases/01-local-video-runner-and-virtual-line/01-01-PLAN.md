---
phase: 01-local-video-runner-and-virtual-line
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - presence_control/__init__.py
  - presence_control/__main__.py
  - presence_control/cli.py
  - presence_control/config.py
  - tests/test_config.py
autonomous: true
requirements:
  - CAL-01
  - CAL-02
  - CAL-04
user_setup: []
must_haves:
  truths:
    - "D-05: First run auto-generates a default config.toml if none exists."
    - "D-07: TOML config is read with Python 3.11 stdlib tomllib."
    - "D-08: Default config path is config.toml in the current working directory."
    - "D-09: CLI supports --config and -c to override the config path."
    - "D-10: CLI args override config values."
    - "D-11: Config sections include [video], [line], and [display]."
    - "D-12: Line coordinates are normalized 0-1 floats."
    - "D-13: Line direction is configured by named endpoints."
    - "User can configure virtual line endpoints and entry direction in TOML."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python project metadata, pytest config, and console entry point"
      contains: "presence-control"
    - path: "presence_control/config.py"
      provides: "Config loading, default generation, normalized line settings"
      min_lines: 80
    - path: "presence_control/cli.py"
      provides: "CLI parsing and CLI-over-config precedence"
      min_lines: 40
    - path: "tests/test_config.py"
      provides: "Coverage for config defaults, TOML parsing, and CLI overrides"
      min_lines: 40
  key_links:
    - from: "presence_control/cli.py"
      to: "presence_control/config.py"
      via: "CLI resolves config path, loads config, then applies overrides"
      pattern: "load_config"
---

<objective>
Create the project scaffold, configuration model and CLI parsing for Phase 1.

Purpose: Establish the reusable settings layer consumed by all later video runner work.
Output: Python package scaffold, generated default `config.toml`, typed config loading, CLI parsing with override precedence, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-CONTEXT.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Python package and CLI entry point</name>
  <files>pyproject.toml, README.md, presence_control/__init__.py, presence_control/__main__.py, presence_control/cli.py</files>
  <read_first>.planning/phases/01-local-video-runner-and-virtual-line/01-CONTEXT.md, .planning/phases/01-local-video-runner-and-virtual-line/01-RESEARCH.md</read_first>
  <action>Create a Python 3.11+ project. In `pyproject.toml`, define project name `presence-control`, dependency `opencv-python`, optional dev dependency `pytest`, and console script `presence-control = "presence_control.cli:main"`. Create `presence_control/__main__.py` that calls `presence_control.cli.main()`. Create `README.md` with local install, test, and run commands. In `cli.py`, add `argparse` options `--config`/`-c` with default `config.toml`, and `--source`/`-s` with no default. Do not implement video opening in this plan.</action>
  <verify>Run `python -m compileall presence_control` and `python -m pytest tests/test_config.py` after tests exist.</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = "&gt;=3.11"` and `presence-control = "presence_control.cli:main"`.
    - `presence_control/__main__.py` imports and calls `main`.
    - `cli.py` accepts `--config`, `-c`, `--source`, and `-s`.
    - `README.md` documents `python -m presence_control --source webcam:0` and `python -m presence_control --config config.toml`.
  </acceptance_criteria>
  <done>Package scaffold and CLI parser exist and compile.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TOML config loading and default generation</name>
  <files>presence_control/config.py, config.toml</files>
  <read_first>.planning/phases/01-local-video-runner-and-virtual-line/01-CONTEXT.md</read_first>
  <action>Implement dataclasses or equivalent typed objects for `[video]`, `[line]`, and `[display]`. Use stdlib `tomllib` to read TOML from binary file handles. If the config file does not exist, create it from a deterministic TOML template string and then load it. Include defaults: `[video] source = ""`, `[line] point_a = [0.25, 0.50]`, `[line] point_b = [0.75, 0.50]`, `[line] entry_direction = "A_TO_B"`, `[display] window_name = "Presence Control"`, `[display] show_fps = true`. Validate normalized coordinates are floats in `[0.0, 1.0]` and `entry_direction` is either `A_TO_B` or `B_TO_A`.</action>
  <verify>Run `python -m pytest tests/test_config.py`.</verify>
  <acceptance_criteria>
    - `config.py` imports `tomllib`.
    - Missing config path creates `config.toml` with `[video]`, `[line]`, and `[display]`.
    - Invalid normalized coordinates raise a clear `ValueError`.
    - Invalid `entry_direction` raises a clear `ValueError`.
  </acceptance_criteria>
  <done>Config can be generated, loaded, validated and reused.</done>
</task>

<task type="auto">
  <name>Task 3: Apply CLI-over-config precedence and tests</name>
  <files>presence_control/cli.py, tests/test_config.py</files>
  <read_first>presence_control/cli.py, presence_control/config.py</read_first>
  <action>Add a `resolve_runtime_settings(argv: list[str] | None = None)` function in `cli.py`. It must parse args, load config from `--config`/`-c`, and apply CLI overrides after config loading. `--source`/`-s` must override `[video].source`; if `--source` is omitted, keep the config value for later interactive selection. Add pytest tests proving default config generation, custom config path, CLI source override, normalized coordinate validation, and direction validation.</action>
  <verify>Run `python -m pytest tests/test_config.py`.</verify>
  <acceptance_criteria>
    - Tests cover D-05, D-07, D-08, D-09, D-10, D-11, D-12, and D-13.
    - `resolve_runtime_settings(["--source", "webcam:0"])` returns source `webcam:0`.
    - `resolve_runtime_settings(["-c", "custom.toml"])` reads `custom.toml`.
  </acceptance_criteria>
  <done>Config/CLI behavior is tested and ready for video source integration.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall presence_control`
- [ ] `python -m pytest tests/test_config.py`
- [ ] `python -m presence_control --help` shows `--source`, `-s`, `--config`, and `-c`
</verification>

<success_criteria>
- Project scaffold exists.
- Config uses stdlib `tomllib`.
- First run can generate `config.toml`.
- CLI args override config settings.
- CAL-01, CAL-02 and CAL-04 are covered by real implementation and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-video-runner-and-virtual-line/01-01-SUMMARY.md`.
</output>
