---
phase: 01-local-video-runner-and-virtual-line
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - presence_control/cli.py
  - presence_control/source_selector.py
  - presence_control/video_source.py
  - tests/test_source_spec.py
autonomous: true
requirements:
  - VID-01
  - VID-02
  - VID-03
user_setup: []
must_haves:
  truths:
    - "D-01: Interactive prompt at startup shows available sources with file dialog for video files and camera listing for webcams."
    - "D-02: CLI --source / -s flag overrides the prompt entirely with no confirmation."
    - "D-03: Source syntax supports webcam:0 and path/to/video.mp4."
    - "D-04: Source failures show clear errors and suggest fallback/retry."
    - "CLI source selection can open webcam sources."
    - "CLI source selection can open local video file sources."
  artifacts:
    - path: "presence_control/source_selector.py"
      provides: "Interactive source prompt, webcam listing, and file dialog boundary"
      min_lines: 80
    - path: "presence_control/video_source.py"
      provides: "Source spec parsing and OpenCV VideoCapture wrapper"
      min_lines: 100
    - path: "tests/test_source_spec.py"
      provides: "Source parsing and error behavior tests"
      min_lines: 50
  key_links:
    - from: "presence_control/cli.py"
      to: "presence_control/source_selector.py"
      via: "CLI calls interactive selector only when source is absent"
      pattern: "select_source"
    - from: "presence_control/video_source.py"
      to: "cv2.VideoCapture"
      via: "OpenCV capture creation for webcam index or file path"
      pattern: "VideoCapture"
---

<objective>
Implement source selection and video source opening for webcam and local files.

Purpose: Let a user choose the video stream through CLI or interactive selection and receive useful errors when the source cannot be opened.
Output: Source parser, interactive selector, OpenCV capture wrapper, and tests for source syntax/error behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-CONTEXT.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-RESEARCH.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse source specs and open OpenCV captures</name>
  <files>presence_control/video_source.py, tests/test_source_spec.py</files>
  <read_first>presence_control/config.py, presence_control/cli.py, .planning/phases/01-local-video-runner-and-virtual-line/01-RESEARCH.md</read_first>
  <action>Create `SourceSpec` with `kind` values `webcam` and `file`. Implement `parse_source_spec(value: str)` so `webcam:0` returns webcam index 0 and any other non-empty string is treated as a file path. Reject empty source strings with a helpful error. Implement `open_capture(spec: SourceSpec)` using `cv2.VideoCapture(index)` for webcams and `cv2.VideoCapture(path)` for files. If `isOpened()` is false, release the capture and raise `VideoSourceError` with a message that includes the source and suggests trying another camera index or file path.</action>
  <verify>Run `python -m pytest tests/test_source_spec.py`.</verify>
  <acceptance_criteria>
    - `parse_source_spec("webcam:0")` returns webcam index `0`.
    - `parse_source_spec("videos/test.mp4")` returns file path `videos/test.mp4`.
    - Empty source raises `VideoSourceError` or `ValueError` with actionable text.
    - Failed `isOpened()` raises `VideoSourceError` with fallback/retry guidance.
  </acceptance_criteria>
  <done>Source strings can be parsed and converted into OpenCV capture attempts.</done>
</task>

<task type="auto">
  <name>Task 2: Implement interactive source selection</name>
  <files>presence_control/source_selector.py, tests/test_source_spec.py</files>
  <read_first>presence_control/video_source.py, .planning/phases/01-local-video-runner-and-virtual-line/01-CONTEXT.md</read_first>
  <action>Create `source_selector.py` with `probe_webcams(max_index: int = 5)`, `choose_video_file()`, and `select_source()`. `probe_webcams` should attempt indexes `0..max_index-1`, keep indexes that open successfully, and release every capture immediately. `choose_video_file` should call `tkinter.filedialog.askopenfilename()` with video file filters and return the selected path or empty string on cancel. `select_source` should print available webcam options plus a video file option, read a terminal choice, and return either `webcam:N` or the chosen path. Keep functions mockable; do not call file dialog during import.</action>
  <verify>Run `python -m pytest tests/test_source_spec.py` with mocks for `cv2.VideoCapture`, `input`, and file dialog.</verify>
  <acceptance_criteria>
    - Webcam probing releases every opened capture.
    - Interactive selection can return a listed webcam as `webcam:N`.
    - Interactive selection can return a path from file dialog.
    - Cancelled file dialog returns to prompt or raises a clear cancelled-selection error.
  </acceptance_criteria>
  <done>Interactive selection supports camera listing and file dialog boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI source precedence to selection/opening</name>
  <files>presence_control/cli.py, tests/test_source_spec.py</files>
  <read_first>presence_control/cli.py, presence_control/source_selector.py, presence_control/video_source.py</read_first>
  <action>Update `cli.py` so resolved runtime settings use this order: load config, apply CLI overrides, if final source is non-empty then skip prompt entirely, otherwise call `select_source()`. Add a lightweight `--dry-run-source` or internal callable path for tests if needed, but avoid adding user-facing behavior not documented. Ensure source errors are caught at the CLI boundary and printed with the fallback message before returning non-zero. Do not implement the frame display loop in this plan.</action>
  <verify>Run `python -m pytest tests/test_config.py tests/test_source_spec.py`.</verify>
  <acceptance_criteria>
    - Passing `--source webcam:0` bypasses `select_source()`.
    - Missing source calls `select_source()`.
    - Source open failures are converted into clear CLI errors.
    - Tests cover D-01, D-02, D-03, and D-04.
  </acceptance_criteria>
  <done>Source selection is fully wired and ready for the display runner.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall presence_control`
- [ ] `python -m pytest tests/test_config.py tests/test_source_spec.py`
- [ ] `python -m presence_control --source webcam:999` fails with a useful fallback message
</verification>

<success_criteria>
- User can choose webcam or local file through CLI or interactive prompt.
- CLI `--source` bypasses prompts.
- Invalid sources fail clearly.
- VID-01, VID-02 and VID-03 are covered by real implementation and tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-video-runner-and-virtual-line/01-02-SUMMARY.md`.
</output>
