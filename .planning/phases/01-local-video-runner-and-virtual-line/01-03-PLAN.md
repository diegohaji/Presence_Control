---
phase: 01-local-video-runner-and-virtual-line
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - README.md
  - presence_control/cli.py
  - presence_control/geometry.py
  - presence_control/overlay.py
  - presence_control/runner.py
  - tests/test_geometry.py
autonomous: true
requirements:
  - VID-04
  - CAL-03
user_setup: []
must_haves:
  truths:
    - "D-06: FPS is displayed both as an on-frame overlay and printed to terminal log."
    - "Configured virtual line and direction marker are visible on displayed frames."
    - "The app reads frames until a file ends or the user stops the window loop."
    - "Line endpoints use normalized coordinates converted to current frame pixels."
  artifacts:
    - path: "presence_control/geometry.py"
      provides: "Normalized line endpoint conversion and direction helpers"
      min_lines: 40
    - path: "presence_control/overlay.py"
      provides: "OpenCV drawing for virtual line, direction marker, status and FPS"
      min_lines: 70
    - path: "presence_control/runner.py"
      provides: "Frame loop connecting capture, config and overlay"
      min_lines: 90
    - path: "tests/test_geometry.py"
      provides: "Geometry conversion tests independent of camera hardware"
      min_lines: 35
  key_links:
    - from: "presence_control/runner.py"
      to: "presence_control/video_source.py"
      via: "Runner opens capture from resolved source"
      pattern: "open_capture"
    - from: "presence_control/runner.py"
      to: "presence_control/overlay.py"
      via: "Runner draws overlay on each frame"
      pattern: "draw_overlay"
    - from: "presence_control/overlay.py"
      to: "presence_control/geometry.py"
      via: "Overlay converts normalized line to pixel coordinates"
      pattern: "normalized_line_to_pixels"
---

<objective>
Build the display runner that shows live frames with the configured virtual line, direction marker and FPS/status feedback.

Purpose: Complete Phase 1's user-visible loop before any detection/tracking work begins.
Output: Geometry helpers, overlay renderer, frame runner, README usage examples and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-CONTEXT.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-RESEARCH.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-01-SUMMARY.md
@.planning/phases/01-local-video-runner-and-virtual-line/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement normalized geometry helpers</name>
  <files>presence_control/geometry.py, tests/test_geometry.py</files>
  <read_first>presence_control/config.py, .planning/phases/01-local-video-runner-and-virtual-line/01-CONTEXT.md</read_first>
  <action>Create `normalized_point_to_pixel(point, width, height)` and `normalized_line_to_pixels(line_config, frame_shape)` helpers. The conversion must multiply normalized x by frame width and normalized y by frame height, clamp final pixel coordinates to frame bounds, and preserve endpoint labels A/B. Add helper logic for entry direction labels so `"A_TO_B"` and `"B_TO_A"` can be rendered consistently.</action>
  <verify>Run `python -m pytest tests/test_geometry.py`.</verify>
  <acceptance_criteria>
    - `(0.25, 0.50)` on a 640x480 frame converts to approximately `(160, 240)`.
    - `(0.75, 0.50)` on a 640x480 frame converts to approximately `(480, 240)`.
    - Direction `"A_TO_B"` yields label or arrow from A to B; `"B_TO_A"` yields B to A.
  </acceptance_criteria>
  <done>Normalized line geometry is testable without camera or GUI.</done>
</task>

<task type="auto">
  <name>Task 2: Draw virtual line, direction and FPS overlay</name>
  <files>presence_control/overlay.py, tests/test_geometry.py</files>
  <read_first>presence_control/geometry.py, presence_control/config.py</read_first>
  <action>Implement `draw_overlay(frame, config, fps, status_text)` using OpenCV drawing functions. Draw the virtual line, endpoint labels `A` and `B`, a direction arrow for the configured entry direction, and text for FPS/status. Keep colors and thickness readable but simple. Ensure the function mutates and returns the frame for easy runner use. Do not add detection/tracking boxes in this phase.</action>
  <verify>Run `python -m pytest tests/test_geometry.py` and `python -m compileall presence_control`.</verify>
  <acceptance_criteria>
    - `overlay.py` calls `cv2.line`, `cv2.arrowedLine`, and `cv2.putText`.
    - Overlay function accepts FPS and status text.
    - No person detection, tracking, segmentation, or counting code is introduced.
  </acceptance_criteria>
  <done>Overlay renderer can draw Phase 1 visual diagnostics on any frame.</done>
</task>

<task type="auto">
  <name>Task 3: Implement the frame runner and final CLI wiring</name>
  <files>presence_control/runner.py, presence_control/cli.py, README.md</files>
  <read_first>presence_control/cli.py, presence_control/video_source.py, presence_control/overlay.py</read_first>
  <action>Create `runner.py` with `run(settings)`. It must open the selected source through `open_capture`, loop over frames, compute FPS with `time.perf_counter()`, call `draw_overlay`, show frames in `cv2.imshow(config.display.window_name, frame)`, print periodic FPS/status to terminal when `show_fps` is true, exit cleanly when file ends or user presses `q`, and release capture/destroy windows in `finally`. Update `cli.py` so `main()` resolves settings and calls `run(settings)`. Update README with examples for webcam, file path, custom config, and first-run config generation.</action>
  <verify>Run `python -m compileall presence_control` and `python -m pytest`. Manual smoke test if a webcam/video is available: `python -m presence_control --source webcam:0`.</verify>
  <acceptance_criteria>
    - `runner.py` releases capture in `finally`.
    - `runner.py` calls `cv2.imshow` and handles `q` key exit.
    - File end is treated as a clean stop, not an error.
    - README explains webcam/file/custom-config usage and how to edit normalized line coordinates.
  </acceptance_criteria>
  <done>Phase 1 app can display video frames with line, direction and FPS/status overlays.</done>
</task>

</tasks>

<verification>
- [ ] `python -m compileall presence_control`
- [ ] `python -m pytest`
- [ ] `python -m presence_control --help`
- [ ] If local video hardware/media is available, smoke test webcam and one video file source.
</verification>

<success_criteria>
- Frames display from the selected source.
- The configured line and entry direction are visible.
- FPS/status is visible on-frame and in terminal output.
- The runner exits cleanly and releases resources.
- VID-04 and CAL-03 are covered by real implementation and tests/smoke checks.
</success_criteria>

<output>
After completion, create `.planning/phases/01-local-video-runner-and-virtual-line/01-03-SUMMARY.md`.
</output>
